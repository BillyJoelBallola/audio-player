import { StyleSheet, Text, View } from 'react-native';
import { usePr } from 'expo-av';
import React from 'react';

const SongSlider = () => {
  return (
    <View>
      <Text>SongSlider</Text>
    </View>
  )
}

export default SongSlider;

const styles = StyleSheet.create({});